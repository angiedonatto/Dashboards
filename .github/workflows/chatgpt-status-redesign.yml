name: ChatGPT status redesign

on:
  push:
    branches:
      - chatgpt/status-thermometer

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "workflow available"
